// Util.h
#pragma once

#include <map>
#include <queue>
#include <vector>

#include <string>

#include <memory>

#include <chrono>

#include <fstream>
#include <iomanip>

using namespace std;

using uint = unsigned int;

#define Now chrono::system_clock::now().time_since_epoch().count()